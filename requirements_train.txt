# Training-only deps for 3DDA mimicgen tokenization experiments.
# NO robotics/sim stack (no robosuite/robomimic/mujoco/mimicgen) — remote does
# not run rollouts. Install AFTER torch + flash-attn (see setup_remote_train_env.sh).
numpy<2
diffusers==0.39.0
einops==0.8.2
scipy==1.15.3
opencv-python==4.11.0.86
blosc==1.11.4
typed-argument-parser==1.12.0
ftfy==6.3.1
regex
tqdm
matplotlib
git+https://github.com/openai/CLIP.git
